name: alpha
on:
  push:
  workflow_dispatch:
jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Lateral Probing Agent
        run: |
          python3 .github/workflows/agent.py
          echo "STATUS: LATERAL_PROBE_ACTIVE"
          sleep 3600
