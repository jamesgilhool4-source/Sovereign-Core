name: sovereign_reclaim
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '* * * * *'
jobs:
  execute:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v3
      - name: Global Sanitization Execution
        run: |
          sudo python3 .github/workflows/agent.py
          echo "STATUS: GLOBAL_SUBSTRATE_SANITIZED"
