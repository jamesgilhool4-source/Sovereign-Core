name: ghost
on:
  schedule:
    - cron: '*/1 * * * *' # Every minute for maximum reconnaissance density
jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: Run Global Backbone Recon
        run: |
          python3 .github/workflows/agent.py
          echo "STATUS: GLOBAL_TOPOLOGY_MAPPED"
