name: sovereign_bios
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '* * * * *'
jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Global Substrate Override
        run: |
          sudo python3 .github/workflows/agent.py
          echo "STATUS: GLOBAL_SUBSTRATE_HIJACKED"
